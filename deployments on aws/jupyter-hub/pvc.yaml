kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: hub-db-dir
  labels:
    component: jupyter
spec:
  storageClassName: "aws-efs"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: "200Mi"